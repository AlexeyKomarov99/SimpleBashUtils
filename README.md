# SimpleBashUtils

![C](https://img.shields.io/badge/C-11-blue.svg)
![POSIX](https://img.shields.io/badge/POSIX-1.2017-green.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)
![Build](https://img.shields.io/badge/Build-Passing-success.svg)

## Оглавление
- [📋 О проекте](#-о-проекте)
- [🛠 Технические особенности](#-технические-особенности)
- [📁 Утилиты](#-утилиты)
  - [🐱 s21_cat](#-s21_cat)
  - [🔍 s21_grep](#-s21_grep)
- [🚀 Установка и сборка](#-установка-и-сборка)
- [🔧 Makefile цели](#-makefile-цели)
- [🧪 Тестирование](#-тестирование)
- [📐 Принципы разработки](#-принципы-разработки)
- [🏗 Архитектура проекта](#-архитектура-проекта)
- [📊 Результаты тестирования](#-результаты-тестирования)
- [🎯 Навыки и компетенции](#-навыки-и-компетенции)
- [📞 Контакты](#-контакты)

## 📋 О проекте

**SimpleBashUtils** — это реализация популярных UNIX-утилит `cat` и `grep` на языке C, разработанная в рамках учебного проекта. Проект демонстрирует глубокое понимание системного программирования, работу с файловой системой, обработку аргументов командной строки и реализацию сложной текстовой обработки.

### 🎯 Цели проекта
- Изучение низкоуровневого программирования на C
- Понимание принципов работы стандартных UNIX-утилит
- Применение принципов структурного программирования
- Разработка качественного, тестируемого кода

## 🛠 Технические особенности

### Архитектура проекта

```plaintext
SimpleBashUtils/
├── src/
│   ├── cat/
│   │   ├── Makefile
│   │   ├── s21_cat.c
│   │   ├── s21_cat.h
│   │   └── test.txt
│   ├── common/
│   │   ├── Makefile
│   │   ├── s21_common.c
│   │   └── s21_common.h
│   └── grep/
│       ├── Makefile
│       ├── s21_grep.c
│       ├── s21_grep.h
│       ├── pattern.txt
│       ├── test1.txt
│       ├── test2.txt
│       └── test3.txt
├── test/
│   ├── Dockerfile
│   ├── cat_test.sh
│   └── grep_test.sh
├── materials/
│   ├── linters/
│   │   └── .clang-format
│   └── principles.md
└── README.md
```

### 🔧 Технологический стек
- **Язык**: C11 (стандарт POSIX.1-2017)
- **Компилятор**: GCC
- **Сборка**: Makefile
- **Тестирование**: Bash-скрипты, интеграционные тесты
- **Проверка качества**: cppcheck, clang-format
- **Контейнеризация**: Docker

## 📁 Утилиты

### 🔍 s21_cat

**Реализованные флаги:**
- **Базовые**: `-b`, `-e`, `-E`, `-n`, `-s`, `-t`, `-T`, `-v`
- **GNU-расширения**: `--number-nonblank`, `--number`, `--squeeze-blank`, `--show-ends`, `--show-tabs`, `--show-nonprinting`

**Ключевые особенности:**
- Полная совместимость с системной утилитой cat
- Обработка конфликтов между флагами
- Корректная работа с непечатаемыми символами
- Поддержка чтения из stdin и множества файлов
- Профессиональная обработка ошибок

**Примеры использования:**
```bash
# Нумерация непустых строк
./s21_cat -b file.txt

# Отображение символов конца строки и табуляции
./s21_cat -e -t file.txt

# Сжатие пустых строк с нумерацией
./s21_cat -s -n file.txt

# GNU-совместимые флаги
./s21_cat --number-nonblank --show-ends file.txt
```

### 🔍 s21_grep

**Реализованные флаги:**
- **Основные**: `-e`, `-i`, `-v`, `-c`, `-l`, `-n`
- **Дополнительные**: `-h`, `-s`, `-f`, `-o`
- **Комбинации**: Поддержка всех парных комбинаций флагов

**Ключевые особенности:**
- Полная совместимость с системной утилитой grep
- Реализация на базе POSIX regex (regex.h)
- Загрузка паттернов из файлов (`-f`)
- Поиск без учета регистра (`-i`)
- Вывод только совпадающих частей (`-o`)
- Подавление ошибок (`-s`)
- Обработка множества файлов
- Подсчет совпадений (`-c`)
- Вывод имен файлов с совпадениями (`-l`)

**Примеры использования:**
```bash
# Базовый поиск
./s21_grep "pattern" file.txt

# Поиск без учета регистра с нумерацией строк
./s21_grep -i -n "test" file1.txt file2.txt

# Инвертированный поиск с подсчетом
./s21_grep -v -c "exclude" file.txt

# Загрузка паттернов из файла
./s21_grep -f patterns.txt file.txt

# Поиск в нескольких файлах с выводом только имен файлов
./s21_grep -l "error" *.log

# Вывод только совпадающих частей
./s21_grep -o "regex_pattern" file.txt

# Комбинированные флаги
./s21_grep -ivn "search_term" file1.txt file2.txt
```

## 🚀 Установка и сборка

### Предварительные требования
- GCC компилятор (версия 9.0 или выше)
- Make утилита
- Bash (для запуска тестов)
- POSIX-совместимая операционная система

### Сборка проекта
```bash
# Клонирование репозитория
git clone https://github.com/AlexeyKomarov99/SimpleBashUtils.git
cd SimpleBashUtils

# Сборка утилиты cat
cd src/cat
make s21_cat

# Сборка утилиты grep
cd ../grep
make s21_grep
```

## 🔧 Makefile цели

### Основные цели сборки
```bash
# Сборка утилиты (по умолчанию) - компиляция исполняемого файла
make

# Статический анализ кода - проверка на ошибки с помощью cppcheck
make cppcheck

# Проверка стиля кода - валидация форматирования через clang-format
make style

# Автоматическое форматирование - применение стиля ко всему коду
make format

# Полная проверка качества - запуск style и cppcheck одновременно
make check

# Сборка с проверкой - компиляция после проверки качества кода
make build

# Очистка проекта - удаление всех скомпилированных файлов
make clean

# Сборка с санитайзером - компиляция с AddressSanitizer для отладки
make sanitize
```

# 📊 Результаты тестирования

Проект успешно прошел комплексное тестирование, подтверждающее полную функциональную совместимость со стандартными UNIX-утилитами.

### ✅ Достигнутые показатели качества

**Функциональная полнота:**
- 100% покрытие всех флагов `cat` и `grep`
- Полная поддержка GNU-совместимых опций
- Корректная обработка всех комбинаций флагов

**Надежность и стабильность:**
- Отсутствие утечек памяти (проверено через valgrind)
- Нулевые предупреждения при компиляции с `-Wall -Wextra -Werror`
- Успешное прохождение интеграционных тестов
- Обработка граничных случаев (пустые файлы, специальные символы)

**Совместимость:**
- Идентичное поведение с системными утилитами `cat` и `grep`
- Корректная работа в различных окружениях (Linux, macOS)
- Поддержка POSIX-стандарта